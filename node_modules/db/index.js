var phrases;
exports.connect = function() {
    phrases = require('db/ua');
};

exports.getPhrase = function(name) {
    if (!phrases[name]) {
        throw new Error("Такої фрази не існує: " + name);        
    }
    return phrases[name];
}